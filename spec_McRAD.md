# McRAD 명세 (spec_McRAD)
> 도로 모듈 전용 명세. 공통 토대는 spec_McEZR.md 참조.
> 마지막 업데이트: 2026-06-05 · 현재 버전: v0.2.0

---

## 0. 이 문서의 범위

McRAD(도로 설계 모듈) 한정. 모듈 공통 철학·데이터·제약은 `spec_McEZR.md`에 있다.
여기엔 McRAD에서만 적용되는 확정 결정만 적는다.

---

## 1. 그리기 — Cities: Skylines 방식 (모드 선택형)

긋기 전에 모드를 먼저 고른다. 긋고 나서 바꾸는 방식이 아니다.

| 모드 | 세그먼트 | 그리는 법 |
|------|----------|----------|
| 직선 | line | 두 점 클릭. 90도 스냅 옵션 |
| 곡선 | arc | 시작 → 중심 → 끝, 3점 |
| 자유형 | bezier | 핸들 드래그 |

스냅 단위: 0.25.

---

## 2. 편집 — GIMP/포토샵 펜 도구 방식

그린 뒤 노드 핸들로 다듬는다. 그리기와 편집은 별개의 흐름.

```
Node {
  id, x, y
  handleIn:  {x,y} | null
  handleOut: {x,y} | null
  continuity: 'corner' | 'aligned' | 'symmetric'
}
```

- `corner`: 양쪽 핸들 독립 (기본). 한 세그먼트만 영향.
- `aligned`: 한쪽에 맞춰 방향 정렬 (길이 독립). 좌/우 맞추기.
- `symmetric`: 완전 대칭.

핸들이 null이면 직선, 있으면 베지어. 원호는 type/radius 유지하되 베지어 변환 시 핸들로 환원(곡률 보존).

---

## 3. 도로 연결

### 끝점 스냅 (단계 2)
- 끝점 ↔ 끝점만. **병합 안 함**, 연결 관계만 기록.
- 연결 시 좌표 자동 정렬.

```
Node { ... connections: [ {roadId, nodeId} ] }
```

병합하지 않는 이유: 도로별 고유 속성(도로명·관리주체·도로급) 충돌 방지.

### 연결 유형 5종
| 유형 | 처리 |
|------|------|
| 끝점↔끝점 | 단계 2 (스냅) |
| 끝점↔중간 (T자) | 단계 3 (교차로) |
| 중간↔중간 (십자) | 단계 3 (교차로) |
| 3개 이상 합류 | 단계 3 (교차로) |
| 입체 교차 (겹치나 연결 없음) | 단계 3 (레이어 분리, 교차로 아님) |

---

## 4. 교차로 (단계 3)

T자·십자·다중합류를 **하나의 객체 타입**으로 통합. 연결 도로 수만 다름.

```
Intersection {
  id, name
  type: 'plain' | 'signal' | 'rotary' | 'roundabout'
  position: {x,y}
  connectedRoads: [ {roadId, nodeId} ]   // 개수 제한 없음
  layer                                   // 입체 구분
  boundary: Polygon
  boundaryMode: 'auto' | 'manual'
}
```

### 입체 교차 원칙
입체 교차는 교차로가 **아니다**. 좌표가 겹쳐도 connectedRoads에 없으면 다른 레이어의 두 도로일 뿐.
인터체인지는 거대한 단일 객체가 아니라 **여러 작은 교차로로 분해**된다 (램프 끝마다 별도 교차로).

### 경계 처리
- 자동 계산 후 수동 보정.
- 수동 보정하면 자동 추적 끔(`manual`, 보존). 도로가 바뀌어도 안 따라감.
- "자동 재계산" 명령 = `manual`→`auto` 1회 재계산.

---

## 5. 내보내기

### 좌표계
- 이미지 좌표계 고정, 점 1개 대응 + 1:1.
- 회전 필요 시 좌표축이 아니라 **캔버스를 회전**.
- 1픽셀 = 1블록. 내부 float, 출력 시 정수.

### 변환기 이원화
| 변환기 | 지능 | 역할 |
|--------|------|------|
| 내부 (래스터화 엔진) | 똑똑함 | 곡선 → MC 도로 계단화, 단면을 색 픽셀로 |
| 외부 (서버용 최종) | 멍청함 | 픽셀 → 블록 1:1 |

### 래스터화 — 계단 규칙
- **점접촉 기본**: 모든 대각에서 블록이 모서리만 닿으며 계단.
- **면접촉 예외**: (1) 경계석·중앙분리대처럼 끊기면 안 되는 얇은 요소 (2) 여러 선이 모여 자연히 붙는 경우.
- 폭 일관성: 내부 float 폭을 따라 채워 들쭉날쭉 방지.

### 도로 폭
중심선이 아니라 실제 폭의 면으로 그린다. 변환기는 차선 수 해석 불필요.

---

## 6. 기하-단면 분리 (핵심 구조)

기하(위치)와 단면(가로 구성)을 분리. Cities: Skylines 도로 교체처럼 기하는 두고 단면만 교체.

```
Road {
  id, name, layerId
  nodes[], segments[]              // 기하
  crossSection: {
    semantic: {                    // 의미 — 사용자 지정, 보존
      laneCount, laneWidth, markings, direction
    } | null,                      // null = 순수 기하
    profile: 'basic'               // 표현 — 템플릿, 교체 가능
  }
}
```

3가지 상태: 완전체(semantic+profile) / 의미만(semantic, profile 없음) / 순수기하(semantic=null).
- 프로파일 교체 = profile만 변경, semantic 유지.
- 순수 기하화 = semantic을 null로.

### MVP basic 프로파일
노면 + 차로 수만큼 실선 구분 + 양 끝 경계. 인도 없음. 차로 수·폭 지정 가능.
향후: with_sidewalk, divided, custom (구조 변경 없이 profile만 추가).

---

## 7. 미확정 (실물 보며 정함)

- 노면표시 구체적 종류
- 프로파일 추가 목록
- 교차로 내부 차선 연결 디테일
- 동적 정보(등록/소멸 이력) 파일 포맷
