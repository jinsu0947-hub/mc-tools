# McEZR 공통 명세 (spec_McEZR)
> 마지막 업데이트: 2026-06-03  
> 이 문서는 기억이 휘발되었을 때 재진입점으로 사용한다.  
> 추측으로 채운 내용 없음. 미확정 항목은 명시적으로 표시됨.

---

## 0. 이 문서를 읽는 법

처음 돌아왔다면 순서대로 읽어라.  
특정 모듈 작업 중이라면 해당 모듈 섹션으로 바로 가라.  
코드를 짜기 전에 반드시 **4. 설계 철학**과 **6. 제약 조건**을 먼저 읽어라.

---

## 1. 프로젝트 정체성

**McEZR**는 마인크래프트 도시 개발을 지원하는 게임 외부 정보 처리 도구다.

한 줄 정의:
> "현실적인 마인크래프트 도시를 만들려는 플레이어가, 도로·필지·건물·지형 데이터를 게임 밖에서 설계하고 관리할 수 있도록 돕는 웹 기반 도시 정보 설계 플랫폼"

대상 사용자: 현실적인 도시를 구현하려는 마인크래프트 플레이어 (개발자 본인 포함)

---

## 2. 모듈 구성

| 모듈 | 이름 | 역할 |
|------|------|------|
| McRAD | Road & Area Data | 도로 설계. 노드, 세그먼트, 차선, 교차로 |
| McBIS | Block & Infrastructure System | 블록·필지·건물 관리 |
| McMIS | Monument Information System | 건물/규제 분석, 위치 정보 지원 |
| McHIS | Height & Infrastructure System | 지형, 높이, 터널, 다리 |

**모듈 간 의존 관계:**
```
McRAD → (roads 데이터 생성)
McBIS → (plots, buildings, zoning 생성) → McRAD 데이터 참조
McMIS → (분석 레이어 생성) → McRAD + McBIS + McHIS 데이터 읽기만 함
McHIS → (terrain/height 제공) → 다른 모든 레이어와 참조 연결
```

McMIS는 원본 데이터를 수정하지 않는다. 분석 레이어만 생성한다.

---

## 3. 2026년 목표 (11월 기준)

**올해 목표: McRAD MVP 완성**

다른 모듈은 올해 건드리지 않는다.  
McRAD가 나머지 모듈의 전제 조건이고, 단독으로 결과물이 나오기 때문이다.

### McRAD 단계별 목표

| 단계 | 내용 | 목표 시기 |
|------|------|-----------|
| 1 | 격자 캔버스 위에 도로를 선으로 그리고 저장 | 7월 |
| 2 | 레이어 개념 도입 (도로 종류별 분리) | 8월 |
| 3 | 차선 수 / 속도 등 속성 입력 UI | 9~10월 |
| 4 | 마인크래프트 좌표계 오버레이 또는 내보내기 | 11월 |

**기술 스택 결정: HTML + JavaScript (Canvas API)**  
이유: 설치 없이 브라우저에서 실행, 시각적 피드백 즉시 확인 가능, 이후 웹앱으로 그대로 확장 가능.

---

## 4. 설계 철학 (변경 불가 원칙)

이 항목은 기능 추가나 구현 방식을 결정할 때 항상 먼저 참조한다.

1. **시각 데이터는 필수가 아니다.**  
   지도 화면 없이도 객체를 편집할 수 있어야 한다.

2. **객체는 지도 없이도 수정 가능해야 한다.**  
   목록 → 항목 선택 → 속성 수정 → 저장. 이 흐름이 지도와 독립적으로 동작해야 한다.

3. **모든 시각 요소는 객체에 연결되어 있다.**  
   화면에 그려진 선은 도로 객체의 표현일 뿐이다. 선이 객체가 아니다.

4. **지도는 데이터의 편집 수단 중 하나다.**  
   지도가 주(主)가 아니다. 데이터가 주(主)다.

5. **개발 지정 구역 내부는 빈 공간 없이 의미가 부여될 수 있어야 한다.**  
   개발 구역 내부는 모든 공간이 parcel로 표현된다. 잔여 공간은 존재하지 않는다.  
   단, 구역 외부(바다, 미개발 평지 등)는 필지 지정 대상이 아니다.

6. **고급 내부 구조는 사용자에게 숨긴다.**  
   사용자는 도로·교차로·구역·건물만 본다.  
   내부에 Node, Segment, Lane, Polygon 등이 있더라도 UI에 노출하지 않는다.

7. **사용자는 파일 탐색기 수준의 인터페이스로 정보를 관리할 수 있어야 한다.**

---

## 5. 데이터 구조

### 5-1. 공통 변수 (모든 객체에 적용)

```json
{
  "id": "",
  "parent_id": "",
  "x": 0.0,
  "y": 0.0,
  "z": 0.0,
  "layer": "",
  "category": ""
}
```

### 5-2. 공통 파일 구조

```
project/
├── roads.json
├── blocks.json
├── parcels.json
├── buildings.json
├── terrain.json
├── zoning.json
├── metadata.json
└── analysis/
```

### 5-3. McRAD 객체

| 객체 | 핵심 필드 |
|------|----------|
| Node | id, 좌표, 연결 Road Segment 목록, 교차로 여부 |
| Road Segment | id, 시작/끝 Node, 중심선 Geometry, 차선 수, 차선 폭, 도로 폭, 통행 방향 |
| Lane | id, Parent Road Segment, Lane Index, 시작/끝 Lane Node, Centerline Geometry, 회전 속성 |
| Lane Connection | From/To Lane ID, 회전 유형, 활성 여부 |
| Intersection / Boundary | id, Center Node, Boundary Polygon, Lane Node 목록, 최소 반경 |
| Turn Curve | id, 시작/끝 Lane Node, Curve Type, Control Points, 샘플링 포인트 목록 |

### 5-4. McBIS 객체

| 객체 | 핵심 필드 |
|------|----------|
| Block | id, 외곽 Polygon, 도로 연결 정보, 용도 구역 연결 |
| Parcel | id, 외곽/내부 링, 면적, Zoning, 도로 접근성, 주소 정보 |
| Building | id, Parcel 연결, Footprint Polygon, 층수/층고, 템플릿 참조, 회전/정면 |
| Public/Utility Space | id, Polygon, 유형, Parcel Hole 처리 |
| Superparcel | id, parcel_ids[] |

**McBIS 토지 생성 순서:**
```
road_parcel → block → parcel → superparcel → building
```

### 5-5. McMIS 레이어 (읽기 전용)

| 레이어 | 핵심 필드 |
|--------|----------|
| Building Layer | Footprint, Height, Floors, Plot ID, Use |
| Plot Layer | Polygon, Zoning ID, Plot Area |
| Zoning Layer | Polygon, Zone Type, FAR/BCR/Height Limit |
| Road Layer | Centerline, Width, Road Type |
| Analysis Layer | Violation Type, Building ID, Geometry, Severity |

### 5-6. McHIS 객체

| 객체 | 핵심 필드 |
|------|----------|
| Terrain / Height Map | X, Z 기준 높이(Y), Layer 정보(토양/건설 가능/수역) |
| Terrain Reference | McRAD Node/Road Segment, McBIS Block/Parcel 참조 |

McHIS 전용 추가 변수: `terrain_id`, `height`, `slope`, `surface_type`, `buildable`, `sub_layer`, `connected_nodes`, `connected_blocks`, `connected_parcels`, `terrain_layer`, `analysis_flags`

---

## 6. 제약 조건 (반드시 지켜야 함)

- 좌표 체계 일관성: 모든 툴은 `x, y, z` float 사용. 최종 출력 시에만 Minecraft 정수 좌표로 변환.
- ID 기반 참조: 모든 객체 간 연결은 ID로만 한다.
- 레이어 분리: 레이어별 데이터는 독립적으로 저장, 필요 시 참조 연결.
- 용어 일원화: `Parcel`, `Footprint`, `Centerline`, `Block`, `Center Node`, `Floor Count` — 다른 용어 사용 금지.
- 파일 형식: JSON / CSV 기반.
- McMIS 읽기 전용: McMIS는 원본 데이터를 수정하지 않는다.
- 도로는 임의 생성되지 않는다: McRAD에서 불러온 정보 기반으로만 생성.
- Schematic(.schem) 입력 금지: 서버 건축물 반출 금지 환경 윤리적 이용을 위해.
- 블록 범주: "자연", "토목", "인공" 세 가지만.
- 특수 블록(문, 계단 등) 무시.
- superparcel은 parcel로만 구성된다. 잔여 공간 없음.

---

## 7. 미확정 항목 (채우지 말 것)

- 건물 판정 기준 (Minecraft에서 무엇을 건물로 볼 것인지)
- Importer에서 footprint 추출 시 기준 (외벽 vs 지붕)
- McHIS와 McMIS 3D 시각화 통합 방식 세부 구현
- Room/Wall/Door 추상화 모델의 실제 데이터 구조 통합 방식

---

## 8. 폐기된 아이디어

| 아이디어 | 폐기 이유 |
|----------|----------|
| site_layout 도입 | superparcel이 parcel의 집합이므로 잔여 공간이 존재하지 않음 |
| development pattern 자동 적용 | 폐기 |
| parcel 자동 묶기 | superparcel 생성은 사용자 선택이어야 함 |
| 건물 자동 수정 기능 | 폐기 |
| 템플릿 기반 건물만 수정 대상으로 제한 | 폐기 |
| Schematic(.schem) 기반 입력 | 윤리적 이유로 폐기 |

---

## 9. 다음 할 일 (현재 기준)

1. McRAD 1단계 시작: 격자 캔버스 + 선 그리기 + 저장
2. 기술 스택: HTML + JavaScript + Canvas API
3. 첫 번째 파일: `index.html` 단일 파일로 시작

---

## 10. 이 문서 관리 규칙

- 새로운 결정이 생기면 해당 섹션에 추가한다.
- 미확정 항목이 확정되면 7번에서 제거하고 해당 섹션에 옮긴다.
- 폐기된 아이디어는 이유와 함께 8번에 기록한다.
- 추측으로 내용을 채우지 않는다.

---

## 11. 확정 추가사항 (2026-06-05 이후)

### 11-1. 교차로 객체 확정

교차로는 도로보다 **상위 독립 객체**다. 두 도로가 겹친다고 자동 생성되지 않는다. 사용자가 명시적으로 지정한다.

**이유**: 입체 교차(지하차도, 고가도로)를 레이어로 구현할 때, 자동 생성이면 잘못된 교차로가 생긴다.

**교차로 객체 필드 (확정):**
- id
- 이름
- 유형: 일반 / 신호교차로 / 로터리 / 회전교차로
- 연결 도로 목록 (road id 참조)
- 차선별 연결 정보
- 노면표시 / 유도선 / 안전지대 (미구현, 향후)

**T자 교차로 처리 원칙:**
도로는 하나로 유지된다. 단, 교차로 객체가 그 위를 덮어 사실상 분리된 것처럼 동작한다. 불가피하게 쪼갤 경우, 원호/베지어의 접선 기울기는 반드시 유지된다.

**폐기 확정**: 교차로 자동 생성 — 영구 폐기.

---

### 11-2. 내보내기 구조 확정

**출력 형식: 이미지 (GIMP 방식)**

McRAD는 레이어별로 이미지를 렌더링해 출력한다. 스키매틱 변환기(별도 또는 내장)가 이미지를 받아 마인크래프트 스키매틱 파일로 변환한다.

**출력 레이어 종류:**

| 레이어 | 용도 |
|--------|------|
| 기본 | 도로 형태만. 스키매틱 변환기 입력용 |
| 입체 음영 | 시각 확인용 |
| 도로명 정보 | 라벨 오버레이 |
| 필지 정보 | 구역 경계 오버레이 |

**좌표 원점**: 이미지 내 사전 설정. 스키매틱 변환기가 이 원점을 마인크래프트 좌표에 대응.

**단위**: 1픽셀 = 1블록. 내부 저장은 float, 출력 시에만 정수 변환.

**동적 정보**: 도로 등록/소멸 이력은 이미지와 별도 파일로 저장. 구체적 포맷 미확정.

---

### 11-3. 사용 시나리오 확정

**주요 워크플로우:**
1. 도로망을 대략적으로 긋는다
2. 노드를 드래그해 원하는 형태로 다듬는다
3. 교차로를 수동 지정한다
4. 이미지로 내보낸다
5. 스키매틱 변환기로 마인크래프트 파일 생성
6. 서버에서 스키매틱 불러오기로 붙여넣는다

**검토 기준**: 도로 폭(블록 수), 도로 사이 필지 면적이 의도한 용도(아파트 단지 등)에 적합한지 확인.

**목표**: 마인크래프트 도시 건축 서버에서 부지 생성 속도 향상.

---

## 12. McRAD 곡선 편집 데이터 구조 확정 (2026-06-05)

### 12-1. 노드 기반 핸들 모델 (GIMP/포토샵 펜 도구 방식)

곡선 편집은 GIMP/포토샵의 펜 도구와 동일하게 동작한다.
각 노드는 양쪽으로 핸들(제어점)을 가지며, 핸들을 잡아당겨 곡률을 조절한다.
곡선 위의 점을 직접 끌어 제어점을 역산하는 방식은 채택하지 않는다 (데이터 불안정).

**노드 구조 (확정):**
```
Node {
  id
  x, y                              // float 좌표
  handleIn:  { x, y } | null        // 들어오는 방향 핸들
  handleOut: { x, y } | null        // 나가는 방향 핸들
  continuity: 'corner' | 'aligned' | 'symmetric'
}
```

**핸들 연속성(continuity) 타입:**
- `corner`: 양쪽 핸들 독립. 한 세그먼트만 영향. (기본값)
- `aligned`: 한쪽 핸들에 맞춰 반대쪽 방향 정렬 (길이는 독립). 좌측맞추기/우측맞추기.
- `symmetric`: 양쪽 핸들 완전 대칭.

**세그먼트는 양 끝 노드의 handleOut/handleIn으로 그려진다.**
- 핸들이 null이면 직선
- 핸들이 있으면 베지어
- 원호는 별도 type/radius 유지, 베지어 변환 시 핸들로 환원

### 12-2. 타입 변환 규칙

- 원호 → 베지어: 원호 모양을 근사하는 핸들 자동 생성. 작업 손실 없음.
- 변환 시 곡률 최대한 보존.

### 12-3. 핵심 함의 (A-3 실현)

"교차로로 도로를 쪼개도 기울기 유지"는 이 핸들 연속성으로 데이터화된다.
노드의 continuity가 aligned/symmetric이면, 쪼개진 두 세그먼트의 접선이 자동 유지된다.

### 12-4. 확장성

이 곡선 편집 모델(노드+핸들)은 McBIS의 필지 경계 편집에서 그대로 재사용된다.
도로 전용이 아니라 범용 "경로 편집(path editing)" 모듈로 분리해 구현할 것.

---

## 13. McRAD 그리기 모드 확정 (2026-06-05) — Cities: Skylines 방식

### 13-1. 모드 선택형 그리기

도로를 긋기 전에 모드를 먼저 선택한다. (Cities: Skylines 방식)
긋고 나서 타입을 바꾸는 방식이 아니라, 모드를 미리 골라 그 모드로 긋는다.

| 모드 | McRAD 세그먼트 | 그리기 방법 |
|------|---------------|------------|
| 직선 | line | 두 점 클릭. 90도 스냅 옵션 |
| 곡선 | arc (원호) | 시작 → 중심(apex) → 끝, 3점 방식 |
| 자유형 | bezier | 핸들 조작 (드래그로 곡률 결정) |

### 13-2. 그리기와 편집의 분리

- **그리기 단계**: 모드 선택형 (위 표)
- **편집 단계**: 그린 뒤 노드 핸들로 다듬기 (GIMP/포토샵 방식, 섹션 12)

이 둘은 별개의 작업 흐름이다.
빠르게 긋고(그리기) → 마음에 안 들면 다듬는다(편집). B-4 워크플로 실현.

### 13-3. 단계 1 최종 정의

McRAD 단계 1 "곡선 직접 조작"은 다음을 포함한다:
1. 모드 선택형 그리기 (직선/곡선/자유형)
2. 노드 핸들 기반 편집 (corner/aligned/symmetric 연속성)
3. 원호 → 베지어 변환 시 곡률 보존
4. 스냅 단위 0.25

나머지(핸들 색, 클릭 판정 거리, 버튼 배치 등)는 구현하면서 정한다. (되돌리기 저렴)

---

## 14. McRAD 도로 연결 및 교차로 확정 (2026-06-05)

### 14-1. 끝점 스냅 (단계 2)

도로 A의 끝점을 도로 B의 끝점 근처로 가져가면 자동 연결된다.

**병합하지 않는다.** 두 노드는 독립 객체로 남되 "연결됨" 관계를 기록한다.
연결 시 A의 끝점 좌표를 B의 노드 좌표와 동일하게 자동 정렬한다.

**이유**: 노드를 병합하면 도로별 고유 속성(도로명, 관리 주체, 도로 급)이 충돌한다.

```
Node {
  ...
  connections: [ { roadId, nodeId } ]   // ID 기반 참조
}
```

**스냅 범위**: 끝점 ↔ 끝점만. 도로 중간 연결(T자)은 스냅하지 않고 교차로 도구로만.

### 14-2. 도로 연결 유형 전체 (5종)

| 유형 | 설명 | 처리 단계 |
|------|------|----------|
| 1 | 끝점 ↔ 끝점 | 단계 2 (스냅) |
| 2 | 끝점 ↔ 도로 중간 (T자) | 단계 3 (교차로) |
| 3 | 도로 중간 ↔ 도로 중간 (십자) | 단계 3 (교차로) |
| 4 | 3개 이상 합류 | 단계 3 (교차로) |
| 5 | 입체 교차 (좌표 겹침, 연결 없음) | 단계 3 (레이어 분리, 교차로 아님) |

### 14-3. 교차로 객체 통합 처리 (단계 3)

유형 2·3·4는 **하나의 교차로 객체 타입**으로 통합 처리한다.
연결 도로 수만 다를 뿐 구조는 동일하다. (T자=2개, 십자=4개)

```
Intersection {
  id
  name
  type: 'plain' | 'signal' | 'rotary' | 'roundabout'
  position: { x, y }
  connectedRoads: [ { roadId, nodeId } ]   // 개수 제한 없음, 동일 구조
  layer                                      // 입체 구분
  // 차선 연결, 노면표시, 유도선, 안전지대 — 향후
}
```

### 14-4. 입체 교차 원칙 (A-1 실현)

**입체 교차는 교차로가 아니다.** 좌표가 겹쳐도 connectedRoads에 서로 포함되지 않으면
단지 다른 레이어의 두 도로일 뿐이다.

**고속도로 인터체인지 분해 예시:**
- 고속도로 본선 = 도로 1개 (레이어 A)
- 국도 본선 = 도로 1개 (레이어 B)
- 본선끼리는 연결 관계 없음 = 교차로 아님 (입체로 지나침)
- 램프 = 각각 독립 도로
- 램프 끝 ↔ 본선 연결점 = 각각 별개의 교차로

거대한 단일 "인터체인지 객체"를 만들지 않는다.
하나의 인터체인지는 여러 개의 작은 교차로로 분해된다.
교차로는 항상 "끝점이 어딘가에 연결되는 지점" 하나의 패턴으로 환원된다.
이것이 구현을 단순하게 한다.

---

## 15. 교차로 경계 처리 확정 (2026-06-05)

### 15-1. 경계 생성: 자동 계산 후 수동 보정

교차로는 점이 아니라 면적(경계 폴리곤)을 가진다.
연결 도로들의 끝점에서 경계를 자동 계산한 뒤, 사용자가 수동 보정할 수 있다.
이 경계가 안전지대·유도선 등(A-2)의 바탕이 된다.

### 15-2. 수동 보정 후 도로 변경 시 처리 규칙

사용자가 수동 보정하면, 그 시점부터 자동 추적을 끈다(보정값 보존).
- 도로가 바뀌어도 교차로 경계는 자동으로 따라가지 않는다.
- 어긋남은 사용자가 "자동 재계산" 명령으로 해소한다(보정값 폐기, 현재 도로 기준 재계산).

**이유**: 실제 도로 시공에서도 교차로 형상은 설계값에 강하게 구속되지 않고
현장에서 자연스럽게 결정된다. 도구가 억지로 추적할 이유가 없다.

```
Intersection {
  ...
  boundary: Polygon
  boundaryMode: 'auto' | 'manual'   // manual이면 자동 추적 안 함
}
```

- `auto`: 도로 변화에 맞춰 경계 갱신
- `manual`: 경계 고정 (수동 보정 상태)
- "자동 재계산" 명령 = manual을 auto로 되돌려 1회 재계산

---

## 16. 좌표계·내보내기·래스터화 확정 (2026-06-05)

### 16-1. 좌표계 (단계 4)

- 이미지 좌표계는 **고정**. 점 하나 대응 + 1:1 비율.
- 회전이 필요하면 좌표축을 돌리지 않고 **캔버스(그림판)를 회전**시켜 그린다.
- 데이터의 좌표축은 항상 일정. 회전은 보기/그리기 편의 기능일 뿐.
- 단위: 1픽셀 = 1블록. 내부 float 저장, 출력 시 정수 변환.

### 16-2. 변환기 이원화 (중요)

변환기는 둘로 나뉜다. **외부는 멍청하게, 내부는 똑똑하게.**

| 변환기 | 위치 | 지능 | 역할 |
|--------|------|------|------|
| 내부 변환기 | 툴 내 (그림판 래스터화 엔진) | 똑똑함 | 수학 곡선 → MC 도로 계단화. 단면을 색 픽셀로 표현 |
| 외부 변환기 | 서버용 최종 | 멍청함 | 픽셀 → 블록 1:1. 범용 픽셀-투-스키매틱 |

모든 의미는 이미지 안에 픽셀로 담긴다. 외부 변환기는 픽셀을 블록으로 옮길 뿐이다.

### 16-3. 도로 폭 렌더링 (단계 5)

도로는 중심선이 아니라 **실제 폭의 띠(면)로** 그린다.
1픽셀=1블록이므로 6차선이면 그만큼 두꺼운 면. 변환기는 차선 수를 해석할 필요 없음.
B-6("폭이 적절한지 본다")이 화면에서 바로 충족된다.

### 16-4. 곡선 래스터화 — 마인크래프트 특화 계단 규칙 (단계 5)

곡선은 단순 래스터화가 아니라 **마인크래프트 도로답게 계단화**한다 (내부 변환기 담당).

**점접촉 기본:**
모든 대각(45도 및 임의 기울기)에서 하나의 선은 점접촉이 기본.
블록이 모서리만 닿으며 계단진다.

**면접촉 예외 (두 경우):**
1. 면접촉이 더 가치 있는 요소: 경계석, 중앙분리대 등 눈에 띄지 않으면서 끊기면 안 되는 요소.
   선 두께 반올림(사실상 내림)으로 사라질 만큼 얇아도, 면 연속이 더 중요하면 면접촉.
2. 여러 선이 모여 블록이 자연히 붙는 경우.

**폭 일관성:**
내부 float 중심선+폭이 정확히 유지되므로, 래스터화가 정확한 폭을 따라 채운다.
단순 래스터화의 폭 들쭉날쭉함을 피하는 것이 내부/외부 분리의 목적.

### 16-5. 기하-단면 분리 (핵심 구조)

도로의 기하(geometry)와 단면(cross-section)을 분리한다.
Cities: Skylines의 도로 교체처럼, 기하는 두고 단면만 갈아끼울 수 있다.

단면은 다시 두 층으로 나뉜다:
- **의미 정보(semantic)**: 차로 수, 차로 폭, 노면표시, 통행 방향. 사용자 지정. 템플릿과 무관하게 보존.
- **표현 정보(profile)**: 어떤 색/블록으로 칠할지. 템플릿이 결정. 교체 시 갈림.

```
Road {
  id, name, layerId
  nodes[], segments[]              // 기하 (위치)
  crossSection: {
    semantic: {                    // 의미 — 사용자 지정, 보존됨
      laneCount, laneWidth,
      markings,                    // 노면표시
      direction
    } | null,                      // null이면 순수 기하만
    profile: 'basic'               // 표현 — 템플릿, 교체 가능
  }
}
```

**3가지 상태:**
1. 완전체: semantic 있음 + profile 있음 → 의미대로 칠해진 도로
2. 의미만: semantic 있음 + profile 없음 → 정보는 있되 최소 표현
3. 순수 기하: semantic = null → 선만 남음

- 프로파일 교체 = profile만 바꿈, semantic 유지
- 순수 기하화 = semantic을 null로

### 16-6. MVP basic 프로파일 정의

MVP의 `basic` 프로파일: 노면 + 차로 수만큼 실선 구분 + 양 끝 경계. 인도 없음.
별도 지정 가능: 차로 수, 차로 폭.

향후 프로파일(단계적, (다) 목표): with_sidewalk, divided, custom 등.
구조가 이미 교체를 품고 있어 profile 추가만으로 확장된다.

